name: Type Check (TypeScript)

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  tsc:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: "9"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile
        working-directory: packages/dashboard-vite

      - name: Type check
        run: pnpm typecheck
        working-directory: packages/dashboard-vite
